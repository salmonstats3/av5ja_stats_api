version: "3.9"

services:
  app:
    image: tkgling/salmon-stats-app:latest
    platform: linux/amd64
    ports:
      - $PORT:$PORT
    container_name: salmon_stats_app
    command: [sh, -c, yarn start:prod]
    environment:
      DATABASE_URL: $DATABASE_URL
    depends_on:
      - db
